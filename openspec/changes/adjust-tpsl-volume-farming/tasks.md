# Tasks — adjust-tpsl-volume-farming

Delivery: **single branch, one merge** (user decision 2026-09-17: "merge todo de una" — no chained PRs). The PR1/PR2/PR3 task groups are executed sequentially on one branch as work units; the user accepted a size exception for the PR2 group (~700 lines). Task-group boundaries are sequencing/verification units, not PR boundaries. Each group must leave `yarn typecheck` green across all workspaces. Unit tests stay paused — no new tests; `apps/bot/test/open-trade.test.ts` (lines 117-118, 288-289) must keep compiling. Specs define WHAT (`specs/trade-exits`, `specs/volume-farming`, `specs/volume-stats-api`); `design.md` D1–D9 define HOW.

## Review Workload Forecast

| Field | Value |
| ------- | ------- |
| Estimated changed lines | ~1,300 total (PR1 ~200, PR2 ~700, PR3 ~400) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR1 (config+DB) → PR2 (bot) → PR3 (backend+web); if the 400-line budget is enforced per PR, split PR2 into PR2a (exits/anchoring/adapter) → PR2b (volume writers + Telegram + monitor rename) |
| Delivery strategy | ask-on-risk |
| Chain strategy | stacked-to-main |

```text
Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High
```

## 1. PR1 — Config surface, DB schema, and migration 0003

Scope: `packages/config`, `packages/db`, `.env.example`, `AGENTS.md`, plus the minimum mechanical call-site updates in the bot required to keep typecheck green after config fields are removed (the spread-exit monitor's deleted-input handling is a typecheck-forced subset of D5; the monitor rename and retained-path work stay in PR2).

- [x] 1.1 Add the removed-vars guard at the top of `loadBotConfig` in `packages/config/src/index.ts`: `REMOVED_ENV_VARS = ["OPEN_TRADE_SPREAD_TP_USD", "OPEN_TRADE_SPREAD_SL_USD", "OPEN_TRADE_EDGE_MIN_PROFIT_USD"]`; each defined var throws an error naming the change and the replacement (`OPEN_TRADE_MIN_PROFIT_USD`). Note: `loadBotConfig` has no general unknown-var allowlist — this is an explicit blocklist per design D8. <!-- sdd-owner: implementation -->
- [x] 1.2 Update the `openTrade` config block in `packages/config/src/index.ts`: change `stopLossPercent` default `"3"` → `"2.5"`; remove `spreadTpUsd`, `spreadSlUsd`, `edgeMinProfitUsd` from the interface and parsing (no alias); add `minProfitUsd` (`OPEN_TRADE_MIN_PROFIT_USD ?? "0.05"`, `parseNonNegativeDecimalString`), `maxLossUsd` (`OPEN_TRADE_MAX_LOSS_USD ?? "0.25"`, `parsePositiveDecimalString`), `slippageBps` (`OPEN_TRADE_SLIPPAGE_BPS ?? "2"`, `parseNonNegativeDecimalString`); keep `spreadExitTimeoutMinutes` (env name `OPEN_TRADE_SPREAD_EXIT_TIMEOUT_MINUTES`, default 30) unchanged. <!-- sdd-owner: implementation -->
- [x] 1.3 Update `.env.example` and the `AGENTS.md` env table: remove the three deleted vars, change the `OPEN_TRADE_STOP_LOSS_PERCENT` default to `2.5`, add `OPEN_TRADE_MIN_PROFIT_USD` (0.05), `OPEN_TRADE_MAX_LOSS_USD` (0.25), `OPEN_TRADE_SLIPPAGE_BPS` (2). <!-- sdd-owner: implementation -->
- [x] 1.4 Update the mechanical config plumbing in `apps/bot/src/notifications/telegram-command-poller.ts`: the `openTradeService()` options wiring (~line 395, drop `edgeMinProfitUsd`, add `minProfitUsd`/`maxLossUsd`/`slippageBps`) and the `formatActiveConfigSummary` lines (~lines 629-630, drop the spread-exit and min-edge lines, add the three new knobs, update the SL default). <!-- sdd-owner: implementation -->
- [x] 1.5 Apply the typecheck-forced monitor deletions in `apps/bot/src/trading/spread-exit-monitor.ts` and its call in `apps/bot/src/runtime/polling-loop.ts` (~lines 106-123): remove the `spreadTpUsd`/`spreadSlUsd`/`edgeMinProfitUsd` inputs, the spread-USD comparisons (`spread-exit-monitor.ts:218-224`), and the per-tick `evaluateCapturedEdge` gate (`:160-185`); stop passing the removed config fields from `polling-loop.ts`. Retain untouched: the stale-`closing` recovery sweep and the 30-minute time-stop (`spread_timeout`). Do NOT rename the monitor/file in this PR (deferred to 2.9). <!-- sdd-owner: implementation -->
- [x] 1.6 Add the volume columns in `packages/db/src/schema.ts`: `filledNotionalUsd: decimal('filled_notional_usd', { precision: 24, scale: 8 }).notNull().default('0')` on both `trades` (~line 69) and `tradeLegs` (~line 109), following the money-column convention. Do not touch `trades.status` / `trade_legs.status` enums. <!-- sdd-owner: implementation -->
- [x] 1.7 Hand-write `packages/db/migrations/0003_filled_notional_volume.sql` containing only additive ALTER statements, using `--> statement-breakpoint` markers exactly like `0002_fix_timestamp_defaults.sql`; the file contains only:

  ```sql
  ALTER TABLE `trades` ADD COLUMN `filled_notional_usd` decimal(24,8) NOT NULL DEFAULT 0;
  ALTER TABLE `trade_legs` ADD COLUMN `filled_notional_usd` decimal(24,8) NOT NULL DEFAULT 0;
  ```

  No `drizzle-kit generate`, no enum changes, no data transformations (spec: volume-farming / hand-written additive migration 0003). <!-- sdd-owner: implementation -->
- [x] 1.8 Append the manual journal entry to `packages/db/migrations/meta/_journal.json` mirroring the existing format exactly: `{"idx": 3, "version": "5", "when": <current-ms-epoch>, "tag": "0003_filled_notional_volume", "breakpoints": true}`. Do NOT add a `0002_snapshot.json` / `0003_snapshot.json` — the meta snapshots are deliberately left as-is per the migration-drift landmine (design D6, Open Question 2 resolved: the journal entry is filename/tag only; entry format pinned from the 0002 record). <!-- sdd-owner: implementation -->
- [x] 1.9 Verify PR1: `yarn typecheck` green across all workspaces, including `apps/bot/test/open-trade.test.ts` (lines 117-118, 288-289 use only `takeProfitPercent`/`stopLossPercent` — confirm no removed field is referenced; adjust test literals only if they reference removed fields, per design D9). <!-- sdd-owner: implementation -->
- [x] 1.10 Verify the migration on a scratch database: `yarn db:migrate` applies 0003 cleanly on (a) a fresh DB and (b) a copy of an existing DB with prior trades; `SELECT filled_notional_usd FROM trades` / `trade_legs` returns `0` for existing rows; `SHOW COLUMNS` confirms both columns and no status-enum changes. Success criterion 7 (fresh half). <!-- sdd-owner: implementation -->

## 2. PR2 — Bot trading core: anchoring, edge band, RISEx adapter, volume writers, Telegram surfaces

Scope: `apps/bot/src/trading/`, `apps/bot/src/exchanges/risex/`, `apps/bot/src/notifications/`, `apps/bot/src/runtime/`, plus `docs/exchanges/risex-integration.md` and `specs/bot.md` wording. Depends on PR1 (config knobs and DB columns exist).

- [ ] 2.1 RISEx MARK_PRICE triggers (design D3): in `apps/bot/src/exchanges/risex/risex-execution-adapter.ts` change `stop_price_option: StopPriceOption.LastTradedPrice` → `StopPriceOption.MarkPrice` at both the `placeTakeProfit` and `placeStopLoss` call sites (~lines 215, 224). Extended stays untouched (`triggerPriceType: "LAST"` + `MARKET_CROSSING_BUFFER_BPS`). Update the trigger-source note in `docs/exchanges/risex-integration.md`. Success criterion 6 (trigger half). <!-- sdd-owner: implementation -->
- [ ] 2.2 RISEx true-fill price with provenance (design D2; Open Question 1 resolved during tasks: the SDK `InfoClient` in `apps/bot/src/exchanges/risex/sdk/InfoClient.ts` already wraps `GET /v1/orders` (`getOrderHistory`, entries carry `price`/`filled_size`, no average-fill field) and `GET /v1/trade-history` (`getAccountTradeHistory`, per-fill `price`), but neither is used by `waitForMarketFill`, and neither is documented to return a per-order average fill price — so a documented fallback plus integrity check IS required). Rework `waitForMarketFill` (`risex-execution-adapter.ts:415-485`) to return `{ priceUsd, source }` where source is `"order_ack"` (ack `average_fill_price` already normalized by `normalizeSubmittedOrder`), `"order_history"` (`/v1/orders` or `/v1/trade-history` read by order id, qty-weighted average of fills), or `"position_average"` (`readPositionEntryPrice`, existing behavior). Keep the timeout fail-closed semantics (`status: 'new'` on timeout). <!-- sdd-owner: implementation -->
- [ ] 2.3 Per-leg fill anchoring in `apps/bot/src/trading/open-trade.ts` `runEntry()` (design D1): after `longEntry`/`shortEntry` resolve (~lines 668-674) and before `store.transition(token, "protecting", ...)` (~line 717), compute per-leg triggers with the existing `applyPercentChange`: long `longTp = longEntry × (1+TP%)`, `longSl = longEntry × (1−SL%)`; short `shortTp = shortEntry × (1−SL%)`, `shortSl = shortEntry × (1+TP%)`. Replace the cross-anchor `protect(..., longSl, longTp)` for the short leg (~lines 764-775) with `protect(..., shortTp, shortSl)`. Success criterion 2. <!-- sdd-owner: implementation -->
- [ ] 2.4 Add the pure helper `assertProtectionAnchors(input)` (new, in `open-trade.ts` or `packages/domain`) with a named constant `PROTECTION_TOLERANCE_BPS = 100` (design D1): assert each trigger within 100 bps of `fill × (1 ± p)` and the cross-symmetry property `|shortSl − longTp|/longTp ≤ 100 bps`, `|shortTp − longSl|/longSl ≤ 100 bps`; log the full anchor set on pass. On breach throw `ProtectionAnchorError`, which must land in the existing `runEntry` catch (cancel working orders, `store.claimRollback(token)`, emergency reduce-only closes, `failed`/`unhedged`, `notifyUrgent`) — no new failure machinery. Success criteria 2, 6. <!-- sdd-owner: implementation -->
- [ ] 2.5 Fill-price integrity check in `runEntry()` (design D2): if the hedge fill provenance is `order_ack` use it directly; if `position_average` (or `order_history` unavailable), treat a blank (`""`) `avg_entry_price`, a non-positive value, a signed-size-derived fallback (flagged/logged by the adapter), or a value failing the 100 bps same-BTC sanity bound against the limit leg's fill as a loud protection-step failure (same `ProtectionAnchorError` path — urgent notify + rollback, never a silently mis-anchored order). Success criterion 6. <!-- sdd-owner: implementation -->
- [ ] 2.6 Fee-aware fill-time edge band in `runEntry()` (design D4): replace the informational `evaluateCapturedEdge` block (`open-trade.ts:680-715`) with `expectedConvergenceUsd = max(0, longEntry − shortEntry)` and `breakevenUsd = entryFees + exitFees + slippage` — entry fees maker-bps on the limit leg + taker-bps on the hedge leg, exit fees taker-bps per leg's venue, all from `options.fees`; slippage = exit notional × `options.slippageBps`/1e4. Remove the `EXIT_SLIPPAGE_BPS` constant (`open-trade.ts:78`) and the `edgeMinProfitUsd` option field (`open-trade.ts:118`). Success criterion 3. <!-- sdd-owner: implementation -->
- [ ] 2.7 Edge abort path + `edge_closed` reactivation (design D4): when `expectedConvergenceUsd < breakevenUsd + options.minProfitUsd`, immediately `closeTradeBothLegs({ ..., reason: "edge_below_cost", legs: [...entryPriceUsd: longEntry/shortEntry...] })` via the existing `apps/bot/src/trading/trade-close.ts` machinery, then return `{ outcome: "edge_closed", ... }` from `runEntry`. Verify the dormant handling in `telegram-command-poller.ts:479-559` (`reportTradeOutcome` → `formatEdgeClosedNotice`) surfaces the Telegram edit with no new plumbing. Success criterion 3. <!-- sdd-owner: implementation -->
- [ ] 2.8 `OPEN_TRADE_MAX_LOSS_USD` runtime assertion on the abort path (design D4): after the abort close, when `realizedPnlUsd` is known, compare `|realizedPnlUsd|` against `options.maxLossUsd`; on exceedance `console.error` + `notifyUrgent` ("fee-model drift detected") but do not throw — the trade is already closed. Success criterion 3. <!-- sdd-owner: implementation -->
- [ ] 2.9 Monitor rename and retained-path verification (design D5; completes what 1.5 started): rename `apps/bot/src/trading/spread-exit-monitor.ts` → `timeout-close-monitor.ts` and `monitorSpreadExits` → `monitorTimeoutClosures`; rename the internal config field `spreadExitTimeoutMinutes` → `openTradeCloseTimeoutMinutes` in config/poller (env var `OPEN_TRADE_SPREAD_EXIT_TIMEOUT_MINUTES` unchanged); drop `priceByExchange` if now unused; keep close reasons `spread_timeout`/`close_recovery` as-is; update the import in `apps/bot/src/runtime/polling-loop.ts` and the spread-exit wording in `specs/bot.md`. Verify the time-stop closes an open trade at 30 minutes and the stale-`closing` recovery sweep still runs. Success criterion 4. <!-- sdd-owner: implementation -->
- [ ] 2.10 Volume writer 1 — entry fills (design D6; spec: volume-farming monotonic coalesce-plus-delta): extend `TransitionDetails` in `apps/bot/src/trading/db-preview-store.ts` with `filledNotionalUsdDelta?: string` (trade level) and a per-leg `filledNotionalUsdDelta?: string`, applied inside `transition()`'s existing transaction via a drizzle `sql` fragment `filledNotionalUsd = coalesce(filledNotionalUsd, 0) + <delta>`; in `runEntry()` pass the limit leg's `settledNotionalUsd` and the hedge leg's `qty × hedge.averageFillPriceUsd` (plus the trade-level sum). Success criterion 5. <!-- sdd-owner: implementation -->
- [ ] 2.11 Volume writer 2 — close fills (design D6): in `apps/bot/src/trading/trade-close.ts`, compute per-leg close deltas `qty × (ack.averageFillPriceUsd ?? position-derived exitPriceUsd)` (existing step-3 fallback) and carry them in the final `store.transition(token, "closed", { legs: [...] })` leg updates so the increments land inside the close transaction. Success criterion 5. <!-- sdd-owner: implementation -->
- [ ] 2.12 Volume writer 3 — venue-side TP/SL closures (design D6): in `apps/bot/src/trading/trade-monitor.ts`, inside the monitor's existing transaction, increment the closing leg's and the trade's `filled_notional_usd` by `qty × exitPriceUsd` using the same `coalesce + delta` fragment; skip the increment when the exit price is unknown. Success criterion 5. <!-- sdd-owner: implementation -->
- [ ] 2.13 Telegram volume surfaces (spec: volume-farming farmed volume surfaced in Telegram notices): add a farmed-volume line to `buildFillSummary` in `apps/bot/src/notifications/telegram-command-poller.ts` (sourced from the persisted `filled_notional_usd`); add the trade's cumulative farmed volume to the close notice in `trade-close.ts`; add a DB-backed lifetime/period farmed-volume line to `/summary` in `apps/bot/src/notifications/trade-summary.ts`, keeping the live-price "Total notional" estimate as a separately labeled line (Open Question 3 recommendation: keep both, labeled distinctly). Success criterion 5. <!-- sdd-owner: implementation -->
- [ ] 2.14 Dead-code sweep: remove the now-unused `evaluateCapturedEdge` export (`open-trade.ts:1126`) if no callers remain after 1.5/2.6, and any orphaned imports; confirm `specs/bot.md` and `docs/architecture.md` exit wording matches the new model where touched. <!-- sdd-owner: implementation -->
- [ ] 2.15 Verify PR2: `yarn typecheck` green across all workspaces and `apps/bot/test/open-trade.test.ts` still compiles (no new tests). Dry-run evidence per design D9: (a) a confirmed dry-run trade logs per-leg anchors + passing cross-symmetry within 100 bps; (b) a forced tolerance breach (temporarily shrink `PROTECTION_TOLERANCE_BPS` in a scratch run) yields urgent notify + `failed` state with no protection orders; (c) a fill below breakeven + $0.05 closes with `edge_below_cost` and fires the `edge_closed` Telegram notice; (d) any spread move before 30 min causes no close, at 30 min `spread_timeout` closes; (e) `OPEN_TRADE_SPREAD_TP_USD=60 yarn dev:bot` fails at config load; (f) `SELECT filled_notional_usd FROM trades` grows at entry, close, and monitor-closure paths. Success criteria 2, 3, 4, 5, 6. <!-- sdd-owner: implementation -->

## 3. PR3 — Backend volume-stats endpoint and web farmed-volume panel

Scope: `apps/backend`, `apps/web`, `specs/backend.md`, `specs/web.md`. Depends on PR1 (DB columns) and PR2 (writers populate them); read-only constraints apply (no mutation routes, no exchange signing in web).

- [ ] 3.1 Backend stats service: create `apps/backend/src/exchanges/volume-stats-service.ts` with `VolumeStatsService` over `getDb()` (mirroring `balance-service.ts`): lifetime total `sum(trades.filled_notional_usd)`; per-venue lifetime `sum(tradeLegs.filled_notional_usd) group by exchangeId`; the same two queries with `where trades.updatedAt >= cutoff` for the 24h / 7d / 30d trailing windows (window filter pinned to `trades.updatedAt`, design Open Question 4). Document in-code the caveat that a trade opened before the window but closed within it contributes its volume to the window. Success criterion 8. <!-- sdd-owner: implementation -->
- [ ] 3.2 Backend normalizer: create `apps/backend/src/exchanges/volume-stats-normalizers.ts` normalizing to the design D7 DTO — `generatedAt` (ISO-8601), `lifetime: { totalUsd, byVenue: [{ exchangeId, volumeUsd }] }`, `windows: { "24h" | "7d" | "30d": {...} }`, decimals as strings per the `formatDecimal` convention. <!-- sdd-owner: implementation -->
- [ ] 3.3 Route wiring: in `apps/backend/src/server.ts` register `app.get('/api/trades/volume-stats', asyncHandler(...))` alongside the balance routes and inject `VolumeStatsService` through the constructor using the same pattern as the balances service. Read-only: no POST/PUT/PATCH/DELETE anywhere; the route performs no order placement or exchange signing. Update `specs/backend.md` with the new route wording. Success criteria 1, 8. <!-- sdd-owner: implementation -->
- [ ] 3.4 Verify backend: against a fresh DB the endpoint returns explicit zero totals; after trades with fills on both venues the lifetime and per-venue sums are consistent with `filled_notional_usd`; an audit of the route table shows only GET routes. Success criterion 8. <!-- sdd-owner: implementation -->
- [ ] 3.5 Web fetch helper: create `apps/web/src/features/dashboard/volume-stats.ts` with `fetchVolumeStats(fetchImpl = fetch)` calling `GET {VITE_BACKEND_API_BASE_URL}/api/trades/volume-stats`, reusing the `getBackendApiBaseUrl()` pattern from `apps/web/src/features/dashboard/exchange-balances.ts` (export/share the helper rather than duplicating it). Success criterion 8. <!-- sdd-owner: implementation -->
- [ ] 3.6 Web panel component: create `apps/web/src/features/dashboard/components/FarmedVolumePanel.tsx` (one component per file): lifetime total plus 24h/7d/30d totals and the per-venue breakdown, using `MetricCard`/`SummaryItem`/`StatusBadge` and `dashboard-formatters.ts`, semantic tokens respected; explicit "$0.00 farmed" empty state when the DB holds zeros (dry-run), never an error. No exchange signing, credential handling, or mutating calls. Update `specs/web.md` panel wording. Success criterion 8. <!-- sdd-owner: implementation -->
- [ ] 3.7 Dashboard wiring: in `apps/web/src/features/dashboard/Dashboard.tsx`, fetch volume stats alongside balances inside the existing 30-second refresh (one effect, `Promise.allSettled`, second promise) with independent loading/error state so a backend outage degrades to balances-only. Success criterion 8. <!-- sdd-owner: implementation -->
- [ ] 3.8 Verify PR3: `yarn typecheck` and `yarn build` green; dry-run evidence (g) `GET /api/trades/volume-stats` returns zeros on a fresh DB and consistent sums after trades; (h) the dashboard renders the farmed-volume panel and re-fetches on the 30 s cadence. Source audit: web app introduces no signing or mutation capability. Success criteria 1, 7 (route-side), 8. <!-- sdd-owner: implementation -->

## Acceptance traceability (proposal success criteria 1–8)

| Criterion | Tasks |
| ----------- | ------- |
| 1. `yarn typecheck` passes across all workspaces | 1.9, 2.15, 3.3, 3.8 |
| 2. Per-leg TP/SL anchored to own fill; tolerance + cross-symmetry logged | 2.3, 2.4 (+ evidence in 2.15a) |
| 3. Edge band abort (`edge_below_cost`, ≪ max loss, `edge_closed` notice) | 2.6, 2.7, 2.8 |
| 4. No spread-USD comparisons; legacy env vars rejected; time-stop + sweep intact | 1.1, 1.5, 2.9 (+ evidence 2.15d, 2.15e) |
| 5. `filled_notional_usd` accumulates at all three writers; Telegram surfaces | 1.6, 2.10, 2.11, 2.12, 2.13 (+ evidence 2.15f) |
| 6. RISEx MARK_PRICE triggers; corrupt/blended fill fails loudly | 2.1, 2.2, 2.4, 2.5 |
| 7. Migration 0003 applies on fresh + existing DB; no new enum values | 1.7, 1.8, 1.10 |
| 8. `GET /api/trades/volume-stats` (read-only) + web panel on 30 s refresh | 3.1, 3.2, 3.3, 3.5, 3.6, 3.7 (+ evidence 3.4, 3.8) |

## Review Workload Forecast

- Chained PRs recommended: Yes
- 400-line budget risk: High
- Estimated changed lines per PR: PR1 ~200, PR2 ~700, PR3 ~400
- Decision needed before apply: Yes
- Notes: Total ≈1,300 changed lines across bot + backend + web; PR2 alone exceeds the 400-line budget (open-trade.ts surgery, RISEx adapter, three volume writers, monitor rename, Telegram surfaces). Decision: accept PR2 as a size exception or split it into PR2a (exits/anchoring/adapter, ~400) → PR2b (volume writers + Telegram + rename, ~300). PR1 must include the typecheck-forced mechanical deletions in `spread-exit-monitor.ts`/`polling-loop.ts` (task 1.5) because removing config fields breaks compilation of those call sites — the semantic rename and retained-path verification stay in PR2 (task 2.9). Deploy order per design Migration Plan: PR1 → `yarn db:migrate` → PR2 → PR3, stacked to main; pre-deploy, remove `OPEN_TRADE_SPREAD_TP_USD` / `OPEN_TRADE_SPREAD_SL_USD` / `OPEN_TRADE_EDGE_MIN_PROFIT_USD` from the deployment env before PR2 rolls out.
